package org.ibit.rol.sac.integracion.ws.provider;

//import javax.security.auth.login.LoginContext;
//import javax.security.auth.login.LoginException;

//import org.apache.commons.logging.Log;
//import org.apache.commons.logging.LogFactory;
//import org.jboss.security.auth.callback.UsernamePasswordHandler;

public class SecurityProvider{
	
	/*private static final Log log = LogFactory.getLog(SecurityProvider.class);
	
	private LoginContext loginContext;
	
	protected void login(String usuario, String password) throws LoginException{
		
		final UsernamePasswordHandler handler = new UsernamePasswordHandler(usuario, password); 
		new LoginContext("seycon", handler).login();
		loginContext = new LoginContext("client-login", handler);
		loginContext.login();
		log.debug("Logueado con exito");
	}

	protected void logout() throws LoginException{
		if(loginContext!=null){
			loginContext.logout();
		}
	}*/
	
}
